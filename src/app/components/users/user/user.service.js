;(function() {
    angular
        .module('user')
        .factory('userService', userService);

    /*ngInject*/
    function userService($firebaseObject, firebaseReference) {
        return {

        };
    }
})();